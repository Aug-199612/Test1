package Generalization;

public interface Superclass1 {//TRAI

	void audioCall();
	void vedioCall();
	void messaging();
	void fiveGService();
	//absolutely similar to the hierachical inheritance but the diff is we use interface
    //superclass must have interface not the class
}
